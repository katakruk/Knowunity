**/.DS_Store
.DS_Store

# Installed npm tools — regenerated by `npm install`, never committed.
node_modules/

# Note: build/ (the generated CSS) is intentionally NOT ignored.
# It is committed so the project builds on a fresh clone.
